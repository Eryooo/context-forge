# ContextForge — 全方位质量指标体系

> **用途**: 作为开发、自审、迭代的唯一基准。每完成一个 Step 回来对照此表自检,
> 所有"可自证"项必须绿;所有"需 MasterGo 验证"项记录验证步骤供用户回来快速点。
>
> **五大维度**: 产品质量、功能质量、体验质量、开发质量、算法质量。
> **评分**: ✅ 达标 | 🟡 部分达标 | ⛔ 未达标 | ⏸️ 需 MasterGo 验证

---

## A. 产品质量(Product Quality)— 与 PRD 的一致性

| 指标 | 验收标准 | 自证方式 | 当前状态 |
|---|---|---|---|
| **PRD 覆盖率** | PRD-TRACEABILITY.md 所有功能模块(A–L)均有对应实现 | 逐项对照 PRD,无遗漏 | ⏸️ Step 1 |
| **边界遵守** | 插件不生成 HTML/React/Vue,不运行代码,只生成数据包(PRD §2.2) | 代码审计,无越界逻辑 | ⏸️ |
| **MVP 优先级遵守** | 规则识别优先/AI 后置;Prompt/JSON 优先/ZIP 后置;关系卡片列表优先/流程图后置 | 实现顺序与 PRD §MVP 优先级一致 | ⏸️ |
| **降级链路完整** | DSL 三层(codegen→节点树→摘要);HTML 失败不阻断;AI 失败回退本地 | 每条降级路径有代码+测试 | ⏸️ |
| **用户旅程匹配** | PRD §4.1 标准旅程 16 步可完整走通 | 需 MasterGo 实操 | ⏸️ 需验证 |

## B. 功能质量(Functional Quality)— 核心功能正确性

### B1. 数据采集层

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| 选区读取准确性 | 读到的 id/name/type/isVisible/width/height 与 MasterGo 展示一致 | 需 MasterGo 对照 | ⏸️ 需验证 |
| 节点树完整性 | 受控遍历(深度/数量上限)下无遗漏;findAll 计数≈递归计数 | 代码逻辑审计 + 探测已证明 | ⏸️ |
| 样式摘要准确性 | fills/strokes/cornerRadius/opacity/AutoLayout 字段与节点实际样式对应 | 需 MasterGo 对照 | ⏸️ 需验证 |
| 文本提取完整性 | TEXT.characters 完整无截断;hyperlinks/textStyles 可选提取 | 需 MasterGo 对照 | ⏸️ 需验证 |
| 截图质量 | PNG 清晰、尺寸合理(默认 scale=1);Uint8Array→base64 转换无损 | base64 解码验证 + 需 MasterGo 目视 | ⏸️ |
| DSL 降级正确性 | codegen 不可用时,节点树 DSL-like JSON 包含足够语义(层级/类型/样式/文本/AutoLayout) | JSON schema 验证 + 对照手册要求 | ⏸️ |

### B2. 语义识别层

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| 页面候选识别召回率 | 真实页面不漏(Frame/Section/设备尺寸节点;命名含"页/首页/列表/详情"的不漏) | 需 MasterGo 真实设计稿验证 | ⏸️ 需验证 |
| 页面候选识别准确率 | 误识别率低(图标/装饰/组件素材/隐藏层/过小节点正确排除) | 需 MasterGo 真实设计稿验证 | ⏸️ 需验证 |
| 页面类型分类准确性 | 13 种类型(entry/home/list/detail/form/modal/drawer/state_*/component/unknown)分类与人工判断一致度≥80% | 需 MasterGo 多样设计稿验证 | ⏸️ 需验证 |
| 弹窗/抽屉识别准确性 | 命名/尺寸/容器归属规则识别准确 | 需 MasterGo 验证 | ⏸️ 需验证 |
| 状态页识别准确性 | 空/加载/错误/成功状态页正确归属到 base 页面 | 需 MasterGo 验证 | ⏸️ 需验证 |

### B3. 关系推断层

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| **4 类关系正确区分** | navigation/overlay/state/process 不混淆 | 代码逻辑审计 + 需 MasterGo 验证 | ⏸️ |
| 置信度评分合理性 | 高(≥0.85)可批量确认;中(0.6–0.84)需确认;低(<0.6)入问题队列;实际分布合理 | 需 MasterGo 多案例验证置信度分布 | ⏸️ 需验证 |
| 原型连线利用率 | reactions 存在时,高置信度关系优先基于此推断 | 代码逻辑审计 | ⏸️ |
| 命名规则覆盖度 | PRD §9.5 典型规则(新增→新增弹窗、编辑→编辑页、删除→删除确认、关闭→closeModal、返回→goBack、列表→详情)全部实现 | 代码中规则表对照 PRD | ⏸️ |
| 待确认问题队列有效性 | 低置信度关系正确进入队列;问题描述清晰,含建议选项 | 需 MasterGo 验证 | ⏸️ 需验证 |

### B4. 数据包导出

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| JSON schema 合规 | package-schema.ts 定义与实际导出 JSON 100% 一致 | JSON.parse + schema 验证器 | ⏸️ |
| Prompt 完整性 | 包含 PRD §12.2 要求的 12 节(任务目标/数据包说明/.../禁止事项/质量检查清单) | 文本审计 | ⏸️ |
| Prompt 禁止事项 | PRD §12.3 的 8 条禁止事项全部写入 | 文本审计 | ⏸️ |
| Markdown 格式正确性 | 标题层级/表格/代码块/列表语法正确,Claude/ChatGPT 可直接解析 | Markdown linter | ⏸️ |
| ZIP 结构正确性 | PRD §11.2 目录结构完整;README/prompt/task/context/prd/pageGraph/interactions/states/quality-report + pages/{pageId}/* + instructions/* | 解压验证 | ⏸️ |

### B5. 质量报告

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| 16 项检查全覆盖 | PRD §14 的 16 项(未命名页面/重复名/未知类型/DSL缺失/HTML缺失/截图缺失/无目标页面关系/弹窗无关闭/详情无返回/表单无成功状态/表单无失败状态/主流程入口缺/PRD缺/Prompt过长/数据包过大)全实现 | 代码逻辑审计 | ⏸️ |
| 评分算法合理性 | blocking 问题→评分<60;warning→扣分;score 反映数据包可用度 | 需多案例验证评分分布 | ⏸️ 需验证 |

---

## C. 体验质量(UX Quality)— 用户操作流畅度与认知负担

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| **5 件事原则** | PRD §4.2:设计师只做 5 件事(选择范围/确认页面/确认关系/补充业务规则/导出);其余自动化 | 操作步骤审计 | ⏸️ |
| 自动推断覆盖率 | 高置信度关系占比≥60%,设计师批量确认即可,不逐条填 | 需 MasterGo 多案例统计 | ⏸️ 需验证 |
| 进度反馈及时性 | 任何超过 1 秒的任务有进度反馈;超过 5 秒的允许取消(PRD §16.3) | 代码审计 + 需 MasterGo 验证响应感 | ⏸️ |
| 深色模式适配 | themeColor=dark 时文字/背景/边框清晰可辨,与 MasterGo 主题协调 | 需 MasterGo 深色下目视(probe-3 已修) | ✅ probe-3 已验证 |
| 错误提示友好性 | 降级/失败时提示清晰(为什么失败/用户要做什么/对结果影响) | 文案审计 | ⏸️ |
| 待确认问题集中度 | 低置信度问题集中在一个队列,不分散在各处让用户反复切换 | UI 流程审计 | ⏸️ |
| 批量操作支持 | 高置信度关系可批量确认;错误关系可批量删除 | 需 MasterGo 验证 | ⏸️ 需验证 |

---

## D. 开发质量(Code Quality)— 代码可维护性与工程规范

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| TypeScript 类型安全 | `tsc --noEmit` 零错误;无 `any`;泛型/联合类型使用合理 | tsc 检查 | ✅ probe-3 通过 |
| Schema 先行 | package-schema/page-graph/interaction/quality-report/ai-settings 五文件冻结在 Step 2,后续模块引用它们,不自造结构 | import 路径审计 | ⏸️ Step 2 |
| 降级链路可追溯 | 每个 dataStatus(dsl/html/screenshot:success\|fallback\|unavailable\|failed)有对应代码路径 | 代码审计 | ⏸️ |
| 错误处理完整性 | try/catch 覆盖所有 async 调用;主流程不因单项失败中断 | 代码审计 | ⏸️ |
| 消息协议类型化 | messages/sender.ts 强类型;主线程/UI 消息用 enum 不用魔法字符串 | 代码审计 | ✅ probe-3 |
| 性能保护 | 受控递归(maxDepth/maxChildren);大画布分包提示;截图数量上限 | 代码逻辑审计 | ⏸️ |
| 构建产物正确性 | `npm run build:npm` 成功;dist/main.js + dist/index.html 存在且非空 | 构建脚本 | ✅ probe-3 |
| 无硬编码依赖外部服务 | AI API 可配置;不假设用户有 OpenAI Key;默认不发完整 DSL | 代码审计 | ⏸️ Step 8 |

---

## E. 算法质量(Algorithm Quality)— 识别与推断的准确性

| 指标 | 验收标准 | 自证方式 | 当前 |
|---|---|---|---|
| 页面候选识别 F1 | 真实案例 F1≥0.85(召回率×准确率调和平均) | 需多设计稿验证集 | ⏸️ 需验证 |
| 页面类型分类准确率 | 与人工标注一致度≥80% | 需标注验证集 | ⏸️ 需验证 |
| 关系推断准确率 | 高置信度关系(≥0.85)的准确率≥90%;中置信度(0.6–0.84)≥70% | 需标注验证集 | ⏸️ 需验证 |
| 置信度校准 | 置信度 0.9 的关系,实际准确率接近 90%(校准曲线斜率≈1) | 需大量案例统计 | ⏸️ 需验证 |
| 命名规则鲁棒性 | 中英文/大小写/简写/多词混合命名均能识别 | 单元测试 + 需真实案例 | ⏸️ |
| 边界 case 处理 | 空选区/单节点/超大画布/全隐藏层/命名混乱画布不崩溃,给合理提示 | 边界测试 + 需 MasterGo 验证 | ⏸️ |

---

## F. 集成验证(Integration Verification)— 需在 MasterGo 真实设计稿验证的清单

> 这些项无法在 CLI 自证,需用户回来在 MasterGo 操作,并复制诊断报告。

### F1. 基础流程验证

1. **空选区启动** — 插件能正常打开,提示选择节点
2. **选中单个 FRAME** — 能读取并识别为页面候选
3. **选中多个 FRAME** — 全部识别,不遗漏
4. **选中 Section 容器** — 能遍历其下 Frame
5. **一键生成 AI 数据包** — 16 步旅程完整走通
6. **深色模式** — 主题跟随,文字清晰
7. **进度反馈** — 采集/识别/推断过程有进度显示

### F2. 多样设计稿验证(需≥3 个不同类型项目)

- **中后台项目**(列表/详情/表单/弹窗/抽屉/状态) — 关系推断准确率
- **营销落地页**(长页面/多状态) — 页面识别准确率
- **移动端 App**(Tab/导航/浮层) — overlay 与 state 区分准确性

### F3. 边界 case 验证

- 命名混乱画板(Frame1/Frame2/矩形 23) — 算法不崩溃,给提示
- 超大画布(>500 节点) — 分包提示
- 全英文 / 全中文命名 — 规则适配

### F4. 导出物验证

- 复制 Prompt 给 Claude Code — 能否直接理解并生成 Demo
- JSON 导出 — 结构完整,无 undefined/null 意外
- ZIP 解压 — 目录结构正确,README 清晰

---

## G. 迭代收敛判定(Definition of Done)

**所有可自证指标(A–E 中标"可自证")达 ✅**,且:
- PRD-TRACEABILITY.md 所有功能模块实现完成
- 构建通过 + typecheck 零错误
- 核心降级链路(DSL/HTML/AI)有代码实现

**需 MasterGo 验证项(F)** 整理成清单(操作步骤 + 预期结果),供用户回来快速验证。

**Red Team 审计通过**: 代码 vs PRD 一致性、文档 vs 实际一致性、声称 vs 能力一致性 — 无虚报、无遗漏、无硬伤。

---

**当前状态**: Step 1 进行中,指标体系已定义,开始执行 Step 2–10。
