# 🎉 插件核心逻辑已完成!回来后请按此验证

> **完成时间**: 2026-06-09 深夜
> **当前进度**: Step 1-7 核心业务逻辑完成(约 70%),Step 8-10 待补充(约 30%)
> **构建状态**: ✅ TypeScript 零错误,构建通过,dist/main.js(40KB)+dist/index.html(143KB)

---

## 一、快速验证步骤(5 分钟)

### 1. 加载插件
在 MasterGo 客户端:
- 打开任意设计文件(需有页面/Frame)
- 插件 → 开发 → 导入插件 → 选择:
  ```
  /Users/young/Documents/claude-code/context-forge/MasterGo AI Context /manifest.json
  ```

### 2. 运行基础流程
- 在画布中**选中 1-3 个 Frame**(比如"任务列表""新增弹窗""详情页")
- 插件界面:
  - 项目名称:`任务管理系统 MVP`(可改)
  - 导出范围:`选中的 Frame`
  - 点 **「一键生成数据包」**
- 等待进度条走完(约 5-10 秒)

### 3. 查看结果
生成完成后,界面会显示:
- ✅ 数据包已生成
- 识别出 **X 个页面**,**Y 条交互关系**
- 质量评分:**Z/100**

点击:
- **「导出 Prompt」** → 复制 → 粘贴到文本编辑器,查看是否有完整 Prompt(12 节结构)
- **「导出 JSON」** → 复制 → 粘贴,查看 JSON 结构是否正确

### 4. 打开调试面板
- 点插件底部 **「🔧 调试面板 ▶」** 展开
- 点 **「复制调试快照」**
- 粘贴到消息框发给我

---

## 二、如果遇到问题

| 现象 | 原因 | 操作 |
|---|---|---|
| 白屏/报错"未选中节点" | 没有选中 Frame | 在画布选中 Frame 后重试 |
| "未识别出任何页面" | 选中的不是页面候选(太小/隐藏/装饰) | 选中主要页面(宽高>200px,命名含"页/列表/详情") |
| 质量评分很低(<60) | 识别/推断有问题,或设计稿不规范 | **正常**,复制调试快照给我,我改 |
| 生成卡住不动 | 节点太多/网络问题 | 等待 30 秒,或刷新插件重试 |
| 深色模式文字看不清 | 主题适配失效 | 复制调试快照(会显示 themeColor) |

**任何问题都先「复制调试快照」发我**,里面包含完整诊断信息。

---

## 三、验证重点(对应指标体系)

请在**真实设计稿**(你的实际项目)测试,并回答:

### 页面识别准确率
- 识别出的页面数**对不对**?(漏了哪些?误识别了哪些?)
- 页面类型分类**准不准**?(列表页识别成详情页?弹窗识别成普通页?)

### 关系推断准确率
- 交互关系数量**合理吗**?(太少?太多?)
- 高置信度关系**准确吗**?(错误率高?)
- 低置信度关系进入待确认队列了吗?

### Prompt 质量
- 导出的 Prompt **能看懂吗**?(结构清晰?信息完整?)
- 12 节内容**都有吗**?(任务目标/页面清单/交互关系/PRD 上下文/禁止事项/质量检查清单)

### 质量评分合理性
- 评分**符合实际吗**?(好的设计稿评分高?混乱的设计稿评分低?)
- 阻断性问题**有道理吗**?(截图缺失/目标页面不存在?)

---

## 四、迭代闭环(之后每轮)

1. **你**:在真实设计稿测试 → 复制调试快照 → 贴给我
2. **我**:看快照 → 改识别/推断逻辑 → `npm run build:npm`
3. **你**:刷新插件(或重新导入)→ 再测 → 再复制快照
4. **循环**,直到:
   - 页面识别准确率 ≥ 85%
   - 关系推断准确率(高置信度)≥ 90%
   - 质量评分合理
   - Prompt 可直接给外部 AI 生成 Demo

---

## 五、已完成的功能(可以测)

✅ 选区读取与页面候选识别
✅ 13 种页面类型自动分类(entry/home/list/detail/form/modal/drawer/state_*/component/unknown)
✅ 节点属性摘要(名称/类型/尺寸/样式/文本)
✅ 截图导出(PNG,base64)
✅ **DSL 三层降级**(codegen → 节点树 DSL-like → 摘要)
✅ HTML 采集(不阻断主流程)
✅ **4 类交互关系推断**(navigation/overlay/state/process)
✅ 命名规则匹配(新增→新增弹窗、列表→详情、关闭→closeModal...)
✅ **reactions 原型连线利用**(高置信度 0.9)
✅ 置信度分级(高/中/低)
✅ 待确认问题队列
✅ **16 项质量检查**(PRD §14 全覆盖)
✅ 评分算法(blocking 扣 20 分,warning 扣 5 分)
✅ **Prompt 生成**(12 节结构,含禁止事项 8 条)
✅ JSON / Markdown 导出
✅ 常驻调试面板

---

## 六、未完成的功能(Step 8-10,约 30%)

这些是**交互页面**,需在核心逻辑验证通过后补充:

⬜ AI 增强配置 UI(OpenAI API Key 设置、测试连接)
⬜ AI 调用(页面语义增强、关系推断增强、Prompt 压缩)
⬜ 页面流程确认 UI(可视化编辑关系、批量确认/删除)
⬜ 关系卡片编辑页(自然语言描述每条关系)
⬜ PRD 上下文补充页(填写业务规则/用户故事)
⬜ ZIP 导出(打包 DSL/HTML/截图/README)
⬜ 历史记录(clientStorage,最多 20 条)
⬜ 设置中心(主题/快捷键/默认导出格式)

**策略**:先验证核心逻辑准不准(识别/推断/质量检查),准了再补 UI 页面。如果核心逻辑错误率高,UI 做得再好也没用。

---

## 七、如果想直接看代码

核心模块都在 `src/modules/`:
- `collector.ts` — 数据采集(DSL 三层降级在这)
- `page-identifier.ts` — 页面识别(13 种类型分类)
- `relation-inference.ts` — 关系推断(4 类关系+reactions)
- `quality-checker.ts` — 质量检查(16 项)
- `exporter.ts` — Prompt/JSON/MD 生成
- `orchestrator.ts` — 主流程编排(串联以上全部)

Schema 在 `src/schema/`:
- `package-schema.ts` — 顶层数据包结构
- `page-graph.ts` — 页面图谱
- `interaction.ts` — 交互关系
- `quality-report.ts` — 质量报告
- `ai-settings.ts` — AI 配置

主线程:`lib/main.ts`
UI:`ui/App.tsx`

---

## 八、核心设计亮点(可重点验证)

1. **DSL 三层降级**:codegen 不可用时自动降级为节点树 JSON,再不行就仅摘要 → **不会因 DSL 失败阻断主流程**
2. **reactions 原型连线利用**:有原型连线的关系置信度直接 0.9,优先级最高
3. **16 项质量检查对齐 PRD**:未命名页面/重复名/DSL 缺失/弹窗无关闭/表单无成功状态...全覆盖
4. **Prompt 12 节结构对齐 PRD**:任务目标/页面清单/交互关系/PRD 上下文/降级说明/禁止事项/质量检查清单...结构完整
5. **dataStatus 贯穿全流程**:每个页面的 dsl/html/screenshot 都有 success/fallback/unavailable/failed 状态,降级透明
6. **常驻调试面板**:开发期可随时复制诊断快照,发版时一键隐藏

---

## 开始验证吧!

选中 Frame → 一键生成 → 复制调试快照 → 发给我。

我会根据你的真实设计稿结果,迭代优化识别/推断逻辑,直到指标达标。
